const state = {
    // startTimeValue: 60,
    // clockString: "01:00"
};

const getters = {
    // get_startTimeValue: (state) => state.startTimeValue,
    // get_clockString: (state) => state.clockString
};

const actions = {
};

const mutations = {};

export default {
    state,
    getters,
    actions,
    mutations
}